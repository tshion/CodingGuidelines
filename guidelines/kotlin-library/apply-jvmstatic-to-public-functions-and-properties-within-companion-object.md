* 原案: `companion object` にある `public` な関数や `const` 以外のプロパティに `@JvmStatic` を設定すること。
* 英訳: Use `@JvmStatic` for `public` functions and `public` non-`const` properties within the `companion object`.
___

## 解説

## 参考文献