* 原案: ダイアログを実装する際、 `android.app.AlertDialog` の代わりに `androidx.appcompat.app.AlertDialog` を使用し、 `DialogFragment` でラッピングすること。
* 英訳: When implementing dialogs, use `androidx.appcompat.app.AlertDialog` instead of `android.app.AlertDialog` and wrap it in a `DialogFragment`.
___

## 解説

## 参考文献
